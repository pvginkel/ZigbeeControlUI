# Backend proxy target (optional override)
BACKEND_URL=http://localhost:3201

# SSE Gateway proxy target (optional override)
SSE_GATEWAY_URL=http://localhost:3202

# Path to tabs YAML config (for iframe proxy setup in dev)
APP_TABS_CONFIG=../backend/test/tabs.yaml
